name: 1 first_helloworld_workflow

on:
  workflow_dispatch

jobs:
  job1_helloWorld_job:
    runs-on: ubuntu-latest
    steps:
      - name: step1_helloWorld_name
        run: echo "Welcome to GitHub Actions"

  job2_multiple_line_commands:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: |
          echo "This is line one"
          echo "This is line two"
