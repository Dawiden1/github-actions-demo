1. Types of triggers
  a) event-bases triggers
  b) manual triggers
  c) scheduledd triggers
  d) workflow triggers
    - push
    - pull request
The elements listed above were used in processes 1–4

2. Working with Variables in Workflows
  a) Variable holds a piece of information (vars can be used in any wf-s across the same repo)
  b) Variable are used for non sensitive data
    ! Secrets are used for sensitive data
To create a variable/secret just go to: Repository Settings > Security an quality > Secrets and variables > Actions

3. Deploying a static Website to an S3 Bucket
  a) Configure S3 ucket to host a static Website

  Firstly, i'm creating a S3 bucket as a static website default page as "index.html",
    making it public, and editing bucket policy letting access to everyone like s3access_policy.json

  b) Write GitHub Actions workflow to deploy to S3 bucket 
  
  7 static deploy to s3 bucket.yml <- action
  For this purpose, I need to create an IAM user granting him an AmazonS3FullAccess Policy,
  and create him a CLI security credentials that allows making changes to S3.
  By default, ubuntu-latest has installed AWS CLI, so that i don't need to install it explicity.

  Overall: http://dawidm-github-actions-demo-bucket.s3-website.eu-west-2.amazonaws.com/

4. Environment Variable 
  a) An environment variable is a key-value pair 
  b) Used for changing eg. deploy details from DEV to TEST (after completing DEV) and then to PROD   

  YAML no. 8 - env demo.yml 

5. Using Inputs/Parameters in a Workflow file
  a) In order to use inputs, the Workflow file must have only manual trigger (workflow_dispatch)
  b) Input can be put in a drop-down list next to the "Run Workflow" button 

  YAML no. 9 and 10

6. Understanding Context Variables in GitHub Actions 
  a) ${{ github.event_name }} - the name of the event that triggered the workflow (e.g., push)
  b) ${{ github.repository }} - the full name of the repository (e.g., owner/repo)
  c) ${{ github.ref }} -        the git ref that triggered the workflow (e.g., refs/heads/main)
  d) ${{ github.actor }} -      the username of the person WHO triggered the workflow
  e) ${{ github.sha }} -        the commit SHA that triggered the workflow 
  f) ${{ github.workflow }} -   the name of the workflow file 

  Docs: https://docs.github.com/en/actions/reference/workflows-and-actions/contexts#github-context 

  YAML no. 11

7. What are outputs in a GitHub Workflow?
  a) Outputs are values returned by one task whick is used by another task 
  b) Using outputs we can share data between tasks/steps 
  c) Sharing data between Jobs

  YAML no. 12

8. Using if condition in Workflow
  a) Using for specified conditions
  b) Example:
    - Run "Build Job" when pull request is raised  
    - Run "Deploy Job" when commits are pushed to main

  YAML no. 13

  Another task for IF conditions using special functions, such as: 
    startswith, endswith, contains, success, failure
  Context:  There will be three branches in use:
  - develop     - deploy the code to development environment
  - main        - deploy the code to production environment
  - feature/**  - static code analysis  